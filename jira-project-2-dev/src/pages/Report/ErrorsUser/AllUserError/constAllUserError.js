export const allUserError = {
    percentBack: 80, // Кол-во процентов
    totalBlocks: 14, // Сколько всего блоков
    blockToShow: 11, // Сколько должно отрисовываться
};

allUserError.height = allUserError.percentBack / 100 * 488;